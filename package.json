{
  "name": "json",
  "version": "0.5.0",
  "description": "An implementation of `JSON.parse()` and `JSON.stringify()` - just for fun. ;-)",
  "author": {
    "name": "David Valles",
    "email": "davidtjvalles@gmail.com"
  },
  "license": "MIT",
  "keywords": [
    "json",
    "parse",
    "stringify"
  ],
  "main": "index.js",
  "scripts": {
    "clean": "rimraf .nyc_output coverage",
    "lint": "eslint index.js lib tests",
    "test": "npm run lint && nyc tape 'tests/**/*.js' | faucet && nyc report",
    "coverage": "nyc report --reporter=html && open coverage/index.html",
    "codecov": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "devDependencies": {
    "codecov": "1.0.1",
    "eslint": "3.13.1",
    "eslint-config-airbnb-base": "11.0.1",
    "eslint-plugin-import": "2.2.0",
    "faucet": "0.0.1",
    "nyc": "10.0.0",
    "rimraf": "2.5.4",
    "tape": "4.6.3"
  },
  "private": true
}
